export type FormData = {
  name: string;
  postalCode: string;
  region: string;
  gender: string;
  hobbies: string[];
  otherHobby: string;
};

export type MasterData = {
  regions: string[];
  hobbies: string[];
};